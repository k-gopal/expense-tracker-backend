const Users = require('expense-libs/models/User');

export const createUser = async (data) => {
    try {
        const newUser = new Users(data)
        const result = await newUser.save()
        if (result) {
            return true
        }
        return false
    } catch (error) {
        console.log("error", error)
        return false
    }
}